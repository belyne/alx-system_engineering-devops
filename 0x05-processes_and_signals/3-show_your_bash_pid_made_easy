#!/usr/bin/env bash
# This script displays the PID and process name of processes whose name contains the word "bash".

# Use pgrep to find processes whose name contains "bash" and display their PID and name
pgrep -l bash
